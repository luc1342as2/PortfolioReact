# Copy to .env.local and fill in your OpenAI key for local dev.

# Required for the local chat API (server/chat-api.js). Get a key at https://platform.openai.com/api-keys
OPENAI_API_KEY=

# Optional: only for production. When building for deploy, set this to your serverless chat API URL (e.g. https://your-app.vercel.app/api/chat). Leave unset for local dev (Vite proxy is used).
# VITE_CHAT_API_URL=

# Contact form: Get your Access Key at https://app.web3forms.com (free). Submissions will be emailed to ghiglilucas@gmail.com.
# VITE_WEB3FORMS_ACCESS_KEY=
